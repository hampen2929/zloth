# zloth Environment Configuration
#
# Copy this file to .env and configure your settings:
#   cp .env.example .env
#
# Generate encryption key:
#   echo "ZLOTH_ENCRYPTION_KEY=$(openssl rand -base64 32)" >> .env

# =============================================================================
# Required Settings
# =============================================================================

# Encryption key for API keys storage (generate with: openssl rand -base64 32)
ZLOTH_ENCRYPTION_KEY=your-encryption-key-here

# GitHub App Configuration (optional - can be configured via Settings UI)
# Create a GitHub App at: https://github.com/settings/apps/new
# Required permissions: Contents (read & write), Pull requests (read & write)
# ZLOTH_GITHUB_APP_ID=
# ZLOTH_GITHUB_APP_PRIVATE_KEY=
# ZLOTH_GITHUB_APP_INSTALLATION_ID=

# Optional: Debug mode
ZLOTH_DEBUG=false

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
ZLOTH_LOG_LEVEL=INFO

# =============================================================================
# Workspace Isolation Mode (Clone-first)
# =============================================================================

# Use git clone for workspace isolation (recommended). When set to 'true',
# zloth creates isolated workspaces via shallow clones instead of git worktrees.
# true: clone mode (default)
# false: legacy worktree mode
ZLOTH_USE_CLONE_ISOLATION=true

# Optional: customize directories (usually not needed)
# ZLOTH_WORKSPACES_DIR=/absolute/path/to/workspaces
# ZLOTH_WORKTREES_DIR=~/.zloth/worktrees  # used only when clone mode is disabled
# ZLOTH_DATA_DIR=~/.zloth/data            # SQLite database location

# =============================================================================
# Model Configuration (optional)
# Configure LLM models via environment variables instead of the Settings UI.
# =============================================================================

# OpenAI
# OPENAI_API_KEY=sk-xxx
# OPENAI_MODEL=gpt-4o

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-xxx
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Google (Gemini)
# GEMINI_API_KEY=xxx
# GEMINI_MODEL=gemini-2.0-flash

# =============================================================================
# Production Settings (for docker-compose.prod.yml)
# =============================================================================

# Your domain for HTTPS (e.g., zloth.example.com)
# ZLOTH_DOMAIN=localhost

# Email for Let's Encrypt certificate notifications
# ZLOTH_ACME_EMAIL=admin@example.com
