# zloth Environment Configuration

# Encryption key for API keys storage (generate with: openssl rand -base64 32)
ZLOTH_ENCRYPTION_KEY=your-encryption-key-here

# GitHub App Configuration
# Create a GitHub App at: https://github.com/settings/apps/new
# Required permissions: Contents (read & write), Pull requests (read & write)
ZLOTH_GITHUB_APP_ID=123456
ZLOTH_GITHUB_APP_PRIVATE_KEY=base64-encoded-private-key
ZLOTH_GITHUB_APP_INSTALLATION_ID=12345678

# Optional: Debug mode
ZLOTH_DEBUG=false

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
ZLOTH_LOG_LEVEL=INFO

# Optional: Custom database path (defaults to {project}/data/zloth.db)
# ZLOTH_DATABASE_PATH=/path/to/zloth.db

# Optional: Custom worktrees directory (defaults to ~/.zloth/worktrees)
# ZLOTH_WORKTREES_DIR=/path/to/worktrees

# =============================================================================
# Model Configuration (optional)
# Configure LLM models via environment variables instead of the Settings UI.
# =============================================================================

# OpenAI
# OPENAI_API_KEY=sk-xxx
# OPENAI_MODEL=gpt-4o

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-xxx
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Google (Gemini)
# GEMINI_API_KEY=xxx
# GEMINI_MODEL=gemini-2.0-flash
